Process notes

Researching tutorials and looking at popular browser translators for inspiration and ideas for features, design etc

Created html, css and js files
First focussed on designing ui
Created sections on html to plan what ill need before coding
Created sections for each box on page
Formatted so that they were next to eachother - process called flexbox

Came up with more ideas:
language selectors would have to be dropdown menus
Could make page navigation bar to an about page, help page etc
If text is too long, scroll down within box or make text smaller to fit
Want to have initial text in boxes that is replaced once user types stuff

Basic dropdowns created - for now, doesn't need to be complicated.
For future - will have to link translation language to dropdown selection.
In future, could make code that if input is detected as English, automatically changes to English

Basic UI created, next need to code user input within textbox that replaces existing text
Created text input using form & textarea. Placeholder text is displayed before user input. Will link input to js to translate the test and display is in output box.

Need to make translation software before making output field else there is no output
Morse code translator created manually
Created morse code and alphabet arrays in js - while it doesn't use APIs, helps build my understanding of js to make using APIs later easier.
Idea is to get input text, identify it with alphabet array, and change it into corresponding morse code array value and present as output.
Needs to work vice versa too

morse translator works
	future features: help page about formatting morse to get correct translation - only appears if morse is selected as input language

Changed code so dropdown values dont have to be defined in html, but in java
- morse code logic still works